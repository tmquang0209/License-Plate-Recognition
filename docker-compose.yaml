version: "3.9"

services:
  lp-recognition:
    ports:
      - "8000:8000"
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    container_name: lp-recognition
    volumes:
      - .:/app
    command: python main.py
